# Скопируй в local.properties и укажи IP своего компьютера в сети.
# Нужно для физического устройства (телефон) — 10.0.2.2 работает только в эмуляторе.
#
# Узнать IP на Mac: ifconfig | grep "inet " | grep -v 127.0.0.1
# Пример: 192.168.1.105

API_HOST=192.168.1.105
